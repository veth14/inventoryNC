# Worship Inventory — React + TypeScript + SWC + Tailwind

This repository is a starter scaffold for the Church Worship Inventory Management System using React, TypeScript, Vite and the SWC React plugin, with Tailwind CSS.

Quick start (PowerShell):

```powershell
cd "C:\Users\Ian Angelo Valmores\Desktop\CodingFiles\WorshipInventory"
npm install
npm run dev
```

Open http://localhost:5173 in your browser.

Want me to run `npm install` and start the dev server for you now? Reply yes and I'll run it in a terminal.
